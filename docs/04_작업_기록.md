# 04 세션 작업 기록 타임라인

설계(Claude) → AI A 구현 → 인계 → AI B 완료 순서와 소스 저장소 버전 ID(Commit Hash)를 기록한다.

## 0. 설계 단계 — Claude Code (Architect / Planner)
- 세션: 2026-08-29 (오전, GMT+9)
- 작업 내용:
  - 사용자 프롬프트 교체 확인 (이전 "검색 필터" T05 → "신선도 배지" T05). 로컬 `realtime-info-board-t04`의 이전 T05 커밋 `57e6c47`(미푸시)은 폐기 대상으로 판단, 사용자 승인 후 미참조.
  - GitHub `stulss/realtime-info-board-t04` `main`(`96d2048`)을 `과제5/realtime-info-board-t05/`로 새로 클론, 브랜치 `feature/t05-freshness-badge` 생성.
  - law.md 3-1 표준 문서 세트를 T05 기준으로 구축. T04 동명 문서는 `docs/T04_*.md`로 보존 복사 후 재작성.
  - 생성: `docs/T05_고정검사_및_상한.md`(고정 검사 10개·상한 동결), `docs/01_기획.md`(신선도 배지 아키텍처·데이터 계약), `docs/00_과제_요구사항_매핑.md`(고정검사 1:1 + 루브릭 5카드), `docs/검증안내서.md`(3단계 육안 검증 + 스크린샷 정책), `docs/AI_A_작업지시.md`, `docs/02·03·04` 스켈레톤.
  - 갱신: `README.md`, `작업내역_체크리스트.md`, `CLAUDE.md`, `AGENTS.md`.
  - 코드 미변경. 계획 문서는 커밋 `fa41377`로 새 private 저장소에 푸시 완료.
- 설계 결정: 6-값 `status` 유지 + 3-값 `dataStatus` 투영 / `ageSeconds` 클라이언트 1초 클록 재계산 / 에러 catch는 기존 `displayPayloadAfterFailure()` 재사용 / 외부 API·DB 무수정 / `?simulate=` 파라미터로 장애 재현 / AI A는 fresh 상태까지·AI B가 장애 분기+E2E.
- 새 저장소 생성: `github.com/stulss/realtime-info-board-t05` (private). `upstream` = 기존 t04.
- T04 베이스 커밋: `96d2048` / 계획 문서 커밋: `fa41377` (main·feature/t05-freshness-badge 푸시 완료)
- 다음: Codex(AI A) 구현.

## 1. OpenAI Codex — Primary Implementer (첫 번째 구현 세션)
- 시작 시각: `2026-08-29 10:40:00 KST`
- 작업 내용 요약: DataStatus/순수 투영 함수와 단위 테스트, 안정 1초 클록, FreshnessBadge, dashboard topbar fresh 연동 및 화면 증거 생성.
- 종료 시각: `2026-08-29 10:55:00 KST`
- 종료 시점 Commit Hash: `90be0ac0350cca26754ce06980c9b6a4d89e0e06`
  *(이 시점의 소스와 `docs/02_인수인계_문서.md`만 AI B에게 전달)*
- 실제 작업 시간: `15`분 / 실제 프롬프트 수: `1`회 (상한 15분·10회)

## 2. Claude Code — Architect/Planner · Secondary Implementer (두 번째 완성 세션)
> 인계 누락 확인:
> - [ ] 누락 없음
> - [x] 누락 있음 (아래 정정 기록 참조)
>
> **정정 1 — 사실과 다른 원인 설명**: `docs/02_인수인계_문서.md` §5 가 `src/fixtures/widgets.ts`, `src/lib/server/http.ts` 를 "물리적으로 누락"으로 기록했으나 두 파일은 실제로 존재했다. 진짜 원인은 `vitest.config.ts` 의 `new URL(...).pathname` 이 비ASCII 경로 `과제5` 를 퍼센트 인코딩해 `@/` 별칭이 깨진 것. `fileURLToPath()` 로 교체해 단위 테스트 29/31 → 45/45.
> **정정 2 — 누락된 요구사항**: T05-C02 는 경과 시간(ageSeconds)의 숫자 표시를 요구하지만 AI A 의 fresh 배지 문구에는 시각만 있고 경과 시간이 없었다. `freshness-badge.tsx` fresh 분기에 `· 경과 {ageSeconds}s` 를 추가.
> **정정 3 — 잠복 버그**: 클라이언트 클록이 서버 수신 시각보다 뒤처지면 `경과 -1s` 가 표시됐다. `freshness.ts` 에서 0 하한 처리 + 회귀 단위 테스트 추가.

- 시작 시각: `2026-08-29 11:54:00 KST` (`2026-08-29T02:54:00Z`)
- 전달된 최초 프롬프트: "이전 작업자가 남긴 `docs/02_인수인계_문서.md`를 바탕으로 다음 작업을 진행해 줘." (실제로는 작업 폴더·브랜치·시작 커밋·구현 범위·수정 금지 목록·검증 명령을 포함한 상세 지시서 형태로 전달됨)
- 작업 내용 요약:
  - `src/lib/client/use-simulate.ts` 신규 — `useEffect` + `window.location.search` 로 `?simulate=timeout|auth|ratelimit|empty` 를 읽고, 각 장애를 기존 `/api/verification/failure` 라우트의 지원 kind 로 매핑하는 `SIMULATE_PLANS` 제공. (`popstate` 재동기화 포함)
  - `src/components/dashboard.tsx` — simulate 전용 Query 추가, 실패 시 기존 `displayPayloadAfterFailure()` 로 이전 정상값 유지. timeout/auth/ratelimit 은 stale, empty 는 이전 값을 넘기지 않아 unavailable.
  - `src/components/freshness-badge.tsx` — fresh 분기에 경과 시간 숫자 표시 추가 (T05-C02).
  - `src/lib/client/freshness.ts` — `ageSeconds` 음수 방지 하한 처리 + `freshness.test.ts` 회귀 테스트 1건 추가.
  - `vitest.config.ts` — 비ASCII 경로에서 `@/` 별칭이 깨지던 문제 수정 (`fileURLToPath`).
  - `tests/e2e/t05-freshness.spec.ts` 신규 — T05-C01~C09 를 5개 테스트로 검증, C10 은 스위트 전체 통과로 충족.
- 종료 시각: `2026-08-29 12:08:00 KST` (`2026-08-29T03:08:00Z`)
- 종료 시점 Commit Hash: `dd87a46d80fdf9bc533ca279840438fb34fc4e33` (구현·문서 커밋)
- 실제 작업 시간: `14`분 (11:54:00 → 12:08:00 KST) / 실제 사용자 프롬프트 수: `1`회 (상한 15분·10회 — **둘 다 상한 내**)
- 참고(상한 항목 아님): 세션 내부 도구 실행 호출은 약 `41`회였다. AI A 의 `1`회와 동일 기준(사용자 프롬프트 수)으로 비교해야 하며, 내부 호출 수는 투명성을 위해 함께 남긴다.

### Claude Code 완성 세션 고정 검사 결과 (T05-C01 ~ T05-C10)

| ID | 결과 | 근거 |
|---|---|---|
| T05-C01 | PASS | E2E `T05-C01·C02·C09` — 배지 `data-status="fresh"`, 텍스트 "fresh". 증거 `T05_01_fresh.png` |
| T05-C02 | PASS | 같은 테스트 — `업데이트 HH:MM:SS` 와 `경과 {n}s` 정규식 검증 |
| T05-C03 | PASS | E2E `T05-C03·C04·C05` — `?simulate=timeout` 에서 `.widget-card` 5개 유지, 이전 정상값 "All Systems Operational" 표시 |
| T05-C04 | PASS | 같은 테스트 — 배지 `data-status="stale"`, "업데이트 지연" 문구 |
| T05-C05 | PASS | 같은 테스트 — 마지막 정상 시각 고정, 3초 후 ageSeconds 증가 확인. 증거 `T05_02_stale_timeout.png` |
| T05-C06 | PASS | E2E `T05-C06` — stale + "권한 오류(401/403)". 증거 `T05_03_stale_auth.png` |
| T05-C07 | PASS | E2E `T05-C07` — stale + "다음 갱신 대기 중". 증거 `T05_04_stale_ratelimit.png` |
| T05-C08 | PASS | E2E `T05-C08` — `unavailable` + "데이터를 가져오지 못했습니다". 증거 `T05_05_unavailable.png` |
| T05-C09 | PASS | E2E `T05-C01·C02·C09` — 색상 속성이 아닌 텍스트 "fresh/stale/unavailable" 와 `aria-label` 로 검증 |
| T05-C10 | PASS | `npm run test:e2e` 14개 전부 통과 (기존 9개 + T05 신규 5개) |

- 고정 검사 실패 오류 회차: `2`회 (E2E 1차 — ageSeconds 음수로 C05 실패 / E2E 2차 — 수정 후 빌드 미갱신으로 동일 실패). 3차에서 14/14 통과.
- 증거 폴더: `docs/검증스크린샷/2026-08-29T03-01-00Z/`

---
### 체크리스트 확인란
- [x] A 시작 → A 인계 → B 시작 → B 완료 순서로 기록됨
- [x] AI B에는 작업 저장소와 AI A 인수인계 문서만 제공됨 (대화 전문 미제공)
- [x] AI B가 받은 인수인계 문서가 AI A가 남긴 내용과 일치함 (내용은 일치하나 §5 원인 설명이 사실과 달라 정정함)
- [x] AI B가 선언한 기능 1개(신선도 배지 + 장애 재현 갱신)를 모두 완성함
- [x] AI B가 고정 검사 10개를 삭제·완화 없이 실행함
- [x] AI B는 AI A와 다른 모델/서비스를 사용함 (AI A = Codex, AI B = Claude Code)

## 3. 통합·공개 상태

- `feature/t05-freshness-badge` → `main` fast-forward 머지·push: `326c7f5f86e77908aab649a43835ba7c87c1245a`
- GitHub 저장소: `https://github.com/stulss/realtime-info-board-t05` — PUBLIC
- 최종 Vercel URL: `https://realtime-info-board-t05.vercel.app/`
- 최종 원격 검증: `2026-08-29T04-21-36Z`, T05 E2E 5/5 PASS 및 상태 5장 육안 확인
- 최종 보고서: 기록 문서 7종+AI 3줄을 바탕으로 `docs/T05_과제_제출_보고서_v3_2026-08-29.pptx` → 동명 PDF 변환, 32장 시각 검토 완료. 역할은 실제 명칭으로, 작업 기록은 커밋 번호 대신 설계·구현·검증 내용으로 작성
- 문서 최신화: 고정검사 10/10, AI A/B 실측치, 인계·비교·배포 상태 반영. `docs/T05_고정검사_및_상한.md`와 기존 테스트 기대값은 무수정.

## 4. 제출 보고서에 반영한 내용형 작업 기록

- **Claude Code — 설계**: fresh/stale/unavailable 상태 규칙, T05-C01~C10 검사 기준, 데이터 계약, 변경 금지 범위와 두 구현 세션의 역할 경계를 정의했다.
- **OpenAI Codex — 1차 구현**: `DataStatus` 타입, 상태 투영 함수, 1초 주입형 시계, 접근 가능한 `FreshnessBadge`, 대시보드 fresh 화면을 구현하고 정상 경로를 검증했다.
- **인수인계**: 수정·생성 파일, 테스트 결과, 남은 문제, 다음 작업, 금지 경계를 7항목으로 기록해 대화 전문 없이 후속 작업이 이어지도록 했다.
- **Claude Code — 후속 완성**: URL 장애 시뮬레이션 4종, 이전 정상값 유지, stale/unavailable 전이, 비ASCII 테스트 경로와 경과 시간 경계값 결함을 수정하고 전체 E2E를 통과시켰다.
- **배포·최종 검증**: 기능 브랜치를 main에 통합하고 Vercel 공개 주소에서 기본 화면과 장애 URL 4종을 열어 5개 상태를 육안 확인했으며 제출 문서를 최신화했다.

> 커밋 해시는 원본 작업 추적을 위한 메타데이터로만 보존하고, 최종 PPT/PDF의 작업 기록에는 사용하지 않는다.