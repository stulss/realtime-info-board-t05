# 03 최종 과제 제출 보고서 및 AI A/B 비교표

## 1. 제출물 링크
- 결과물 주소 (무로그인 공개): `[Vercel 배포 주소]`
- 소스 주소 (GitHub): `[Repository 주소]`

## 2. 짧은 확인 방법 (4줄)
- 어디로 가나요: 메인 대시보드 `/` 및 `/?simulate=timeout`
- 3단계 이내 무엇을 하나요: 우측 상단 신선도 배지의 텍스트·숫자 확인 → 주소창에 `?simulate=timeout` 추가 → 새로고침
- 무엇이 보이면 통과인가요: 장애 재현에도 기존 데이터가 유지되고, 배지만 fresh→stale로 바뀌며 경과 시간이 증가
- 안 될 때 무엇이 보이나요: 빈 화면 또는 "데이터를 불러올 수 없습니다"로 기존 이력까지 사라짐

## 3. AI와 나의 판단 (3줄)
- AI에게 맡긴 일: 퍼플렉시티 조사 기반 장애 시 렌더 유지 + 배지 상태 갱신 로직 구현
- 학생(나)이 직접 판단한 일: 에러 뷰/성공 뷰 분리 없이 단일 뷰에서 배지만 교체하는 패턴 결정
- AI 제안을 따르지 않은 일: (기록 — 없으면 없었던 이유)

## 4. AI A/B 이름 가린 비교표 (측정표)
> 판정 구간에서는 모델/도구 실명을 가리고 AI A, AI B로만 표기한다. 재작업량은 제출 후 채점기가 계산한다.

| 항목 | AI A | AI B |
|---|---|---|
| 사용 모델/도구명 | 가려짐(심사 후 공개) | 가려짐(심사 후 공개) |
| 실제 작업 시간 | `[기록]`분 | `[기록]`분 |
| 프롬프트 요청/호출 수 | `[기록]`회 | `[기록]`회 |
| 고정 검사 실패 오류 회차 수 | `[기록]`회 | `[기록]`회 |
| 고정 검사 최종 통과 수 | 10개 중 `[기록]`개 | 10개 중 `[기록]`개 |

## 5. 다음 작업 도구 선택 기준 (1문장)
- 나의 기준: `[예: 초기 UI 마크업·컴포넌트 생성은 문서화가 강한 도구(AI A)에, 에러 핸들링·로직 버그 해결은 실행·수정 루프가 빠른 도구(AI B)에 맡긴다.]`
