# CLAUDE.md — realtime-info-board-t05

> 현재 과제: **T05 — 데이터 신선도 상태 배지** (베이스: T04 커밋 `96d2048`).
> 세션을 시작하면 가장 먼저 `작업내역_체크리스트.md`를 읽고 그 지침을 따른다.
> 워크스페이스 전체 규칙은 `C:\Users\stuls\Desktop\Agent\law.md`. 충돌 시 law.md 우선.
> T04 시절 문서는 `docs/T04_*.md` 에 보존.

## 1. 세션 시작 시 가장 먼저 할 일
1. `작업내역_체크리스트.md` 한 파일만 먼저 읽는다 (폴더 전체 열람 금지).
2. AI B로 인계받았다면 `docs/02_인수인계_문서.md` 를 읽는다.
3. 구현 작업이면 `docs/AI_A_작업지시.md` + `docs/01_기획.md`.
4. 동결 계약은 `docs/T05_고정검사_및_상한.md` (삭제·완화 금지).

## 2. 절대 통째로 읽지 말 것
| 경로 | 이유 |
|---|---|
| node_modules/, .next/, dist/, build/ | 의존성·빌드 산출물 |
| .git/ | Git 내부 데이터 |
| docs/T04_*.pdf, *.pptx | 대용량 보고서 바이너리 |

## 3. 작업 유형별로 문서 하나만 읽기
| 하려는 작업 | 읽을 문서 |
|---|---|
| 진행 상황·다음 할 일 | `작업내역_체크리스트.md` |
| 인계받은 작업 시작 | `docs/02_인수인계_문서.md` |
| 구현 지시 | `docs/AI_A_작업지시.md` |
| 설계/데이터 계약 | `docs/01_기획.md` |
| 고정 검사·상한 | `docs/T05_고정검사_및_상한.md` |
| 요구사항 전체 | `docs/00_과제_요구사항_매핑.md` |
| 육안 검증·스크린샷 | `docs/검증안내서.md` |
| 배포 | `docs/05_배포.md` |

## 4. 코드 수정 시
- 파일을 통째로 다시 쓰지 않는다 — 필요한 부분만.
- 코드를 고쳤으면 `작업내역_체크리스트.md` 작업 로그·진행 상황을 갱신해야 끝난 것이다.
- `src/app/api/**` 외부 API·DB 코드와 기존 테스트 기대값은 수정하지 않는다.
- 완료 시 law.md 10장 템플릿(Role/Task/Changed/Created/Tests/Result/Problems/Next Agent/Git)으로 보고.
