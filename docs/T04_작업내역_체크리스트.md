# 작업내역 체크리스트

**프로젝트명:** 개인 맞춤형 실시간 정보판 (realtime-info-board)
**단일 진실 공급원(SSOT)**으로서 작업 내역과 의사결정을 누적 기록합니다.

## 1. 진행 상황 체크리스트
- [x] 원본 전략 문서 분석 (`personal-realtime-dashboard-strategy-complete.md`)
- [x] 단계별 실행 계획 수립 (`계획.md`)
- [x] 기존 유사 사이트(로아와) 조사 및 차별점 확인
- [x] Codex 인계용 작업지시서 작성 (`Codex_작업지시서.md`)
- [x] law.md 기준 프로젝트 표준 문서 세트 구축
- [x] 위젯 5개 목록 확정 (작업지시서 구현 요청을 승인으로 반영)
- [x] 0단계 프로젝트 초기화 (Next.js + TypeScript + Tailwind + TanStack Query)
- [x] 1단계 공통 계약·상태별 fixture·반응형/다크 UI
- [x] 2단계 Upbit·GitHub Status 공개 API Route Handler
- [x] 3단계 수출입은행 adapter·공통 timeout/retry·429/503 처리
- [x] 4단계 Lost Ark 공지·거래장 adapter와 options 24시간 캐시
- [x] 5단계 TTL 캐시·dedupe·stale fallback·전체/개별 새로고침·쿨다운
- [x] 6단계 로컬 검증 (lint/typecheck/unit/build/E2E/스크린샷)
- [x] 수출입은행 신규 `oapi` migration·오류 코드 테스트·1시간 캐시 적용
- [x] GitHub 비공개 저장소 연결·첫 커밋·push
- [x] Vercel 배포·서버 전용 키 등록
- [x] 거래장 아이템 검색·환율 통화 선택 UI와 파라미터별 서버 캐시 구현
- [x] 수정 재배포 후 5개 실데이터·Network 키 비노출 최종 검증

## 2. 작업 로그
- **[2026-08-27]** 원본 보존 지시에 따라 `realtime-info-board`를 `realtime-info-board-t04`로 복사하고 원본·복사본 초기 Git 상태와 문서 해시를 대조
- **[2026-08-27]** T04 카드 3~5 보완: 장애 5종 고유 분류, 마지막 정상값 stale/정상값 없는 빈 상태, 다시 시도 복구, Upbit 실제 일봉 2일 기록, KST 날짜 중복 방지, 이전 대비 차이·방향·단위 구현
- **[2026-08-27]** T04 요구사항 매핑·검증 안내서·AI 3줄·전용 체크리스트 갱신; 공개 배포 전 항목은 완료로 허위 표시하지 않음
- **[2026-08-27]** 복사본 `.env.example`에 잘못 들어온 실제 키를 빈 예시값으로 제거하고 소스·빌드·Git 전체 자격증명 패턴 0건을 재검사
- **[2026-08-26]** Claude가 전략 문서를 분석해 0~8단계 계획과 결정 이유를 `계획.md`에 작성
- **[2026-08-26]** 사용자 제공 링크(loawa.com) 확인 — 실시간 서버 혼잡도는 대형 사이트도 제공하지 않음을 재확인, 계획 유지
- **[2026-08-26]** Codex 인계를 위해 `Codex_작업지시서.md` 작성
- **[2026-08-26]** law.md 미준수 지적을 받아 표준 문서 세트(README, 본 체크리스트, docs 8종, CLAUDE.md, AGENTS.md) 구축
- **[2026-08-26]** Codex가 GPT-5.6 Sol을 복잡한 다단계 코딩 권장 모델로 선정하고, 현재 Codex 세션에서 0~5단계 전체 구현
- **[2026-08-26]** `WidgetData<T>` 계약, 5개 Route Handler, 공급자별 adapter, 공통 timeout/retry·캐시·dedupe·stale fallback 구현
- **[2026-08-26]** lint·typecheck·12개 단위 테스트·production build 통과
- **[2026-08-26]** Playwright production E2E 5/5 통과; 공개 API 계약, 브라우저 외부 API 직접 호출 금지, 키 비노출, 모바일·다크 모드 검증
- **[2026-08-26]** 스크린샷 덮어쓰기 금지 피드백 반영: 실행시각별 하위 폴더에 영구 누적하도록 테스트 수정
- **[2026-08-26]** 다음 AI 전에 사용자가 직접 수행할 계정·키·GitHub/Vercel 승인 작업을 `docs/사용자_사전작업_체크리스트.md`로 분리
- **[2026-08-26]** 공식 공공데이터포털 재확인 중 기존 수출입은행 API 도메인이 2026-04-30 종료된 사실 확인; 다음 작업자의 최우선 migration 항목으로 기록
- **[2026-08-26]** 공식 신규 `oapi.koreaexim.go.kr`로 migration하고 RESULT 2/3/4, 빈 일환율 응답, 1,000회/일 제한을 단위 테스트로 검증
- **[2026-08-26]** lint·typecheck·17개 단위 테스트·production build·Playwright 5/5 통과, 누적 스크린샷 세트 `2026-08-26T13-56-43-518Z` 추가
- **[2026-08-26]** GitHub CLI `stulss` 계정을 재인증하고 비공개 저장소 `stulss/realtime-info-board` 연결·첫 push
- **[2026-08-26]** 사용자 Vercel 배포 주소 `https://realtime-info-board.vercel.app/` 수신; 최초 실API 진단에서 공지·Upbit·GitHub 정상, 거래장 HTTP 400, 환율 최근 고시 404 확인
- **[2026-08-26]** `LOSTARK_MARKET_ITEM_NAME`, `EXIM_CURRENCY` 고정 환경변수를 거래장 검색창·21개 환율 통화 목록으로 전환하고 선택값별 서버 검증·캐시 구현
- **[2026-08-26]** 거래장 `CategoryCode: 0`을 제거하고 공식 options의 유효 카테고리 순회 검색으로 수정; adapter 테스트 2건 추가
- **[2026-08-26]** ESLint 10.9.1 시험 설치에서 Next 내장 플러그인 3개의 peer 미지원 확인 후 9.39.5 유지; `unrs-resolver@1.12.2` postinstall 검토·정확 버전 승인
- **[2026-08-26]** Vercel 재배포 전환 중 기본 아이템 404와 일시적 404 콘솔로 원격 E2E 4/5가 두 차례 발생; 두 증거 세트를 보존하고 원인을 각각 수정·재확인
- **[2026-08-26]** 최종 Vercel 원격 E2E 5/5 통과; Lost Ark 공지·거래장·환율·Upbit·GitHub 실데이터와 자체 API 호출·키 비노출 확인

## 3. 주요 결정 사항
- **Next.js + TypeScript + Tailwind + TanStack Query + Vercel**: 로스트아크·기상청 등 키 필요 API를 서버에서만 다루기 위해 Route Handler가 가능한 Next.js 채택 (근거: `계획.md` 1단계)
- **위젯 5개 확정**: 로스트아크 공지, 로스트아크 거래장 시세, Upbit 시세, 수출입은행 환율, GitHub 서비스 상태
- **서버 혼잡도 위젯 미포함**: 공식 API·로아와 모두 실시간 혼잡도 데이터를 제공하지 않음을 확인 (근거: `계획.md` "참고" 섹션)
- **다른 워크스페이스 폴더 미참고**: 사용자 요청에 따라 `dashboard`, `today-status-board` 등 기존 폴더의 코드·패턴을 재사용하지 않고 완전히 새로 설계
- **오류 응답 정책**: 키 미설정·공급자 오류도 공통 계약으로 반환하며 429=호출 제한, 503=공급자 접근 불가, 네트워크=조회 실패로 구분
- **검증 증거 보존**: `docs/검증스크린샷/<UTC 실행시각>/`에 실행별 스크린샷을 새로 추가하고 기존 파일은 수정·삭제하지 않음

## 4. 최종 검증 요약
- `npm run lint`: 통과
- `npm run typecheck`: 통과
- `npm test`: 7개 파일, 19개 테스트 통과
- `npm run build`: Next.js production build 통과, 화면 2개와 API Route 5개 생성 확인
- `npm run test:e2e`: 로컬 production Chrome 5/5 통과
- `PLAYWRIGHT_BASE_URL=https://realtime-info-board.vercel.app EXPECT_LIVE_PROVIDERS=true npm run test:e2e`: Vercel 실데이터 5/5 통과
- 최종 원격 스크린샷: `docs/검증스크린샷/2026-08-26T14-49-09-648Z/` 4장 (SHA-256은 해당 폴더의 `../README.md` 참고)

## 5. law.md / AGENTS.md 준수 점검
- [x] `law.md`를 최초 확인하고 보호 문서를 수정하지 않음
- [x] 워크스페이스 Skill 구조에서 관련 없는 Skill 전체 열람을 피하고, 모델 선택에 필요한 공식 OpenAI 문서만 확인
- [x] `_ai/codex/INSTRUCTIONS.md`와 `_shared` 개발·Git·테스트·보안 지침 확인
- [x] 루트와 프로젝트 `AGENTS.md`, 본 SSOT, 작업지시서, 계획, 요구사항 매핑을 구현 전에 확인
- [x] Git 상태·diff를 점검하고 비공개 GitHub 저장소를 초기화·커밋·push
- [x] 기존 테스트 삭제·무력화 없이 lint/typecheck/unit/build/E2E 수행
- [x] 비밀 키를 서버 환경변수로만 참조하고 `.env*`, key/credential 파일 제외 규칙 확인
- [x] 단계 완료 후 검증 스크린샷을 누적 보존하고, 코드 변경과 관련 문서를 같은 작업에서 최신화
- [x] 최초 스크린샷 덮어쓰기 문제와 실패 검증도 숨기지 않고 트러블슈팅·증거 색인에 기록

## 6. 다음 작업자에게 (Next Prompt)
**다음 작업자: agy 또는 Claude(독립 최종 검토·보고서 준비)**

> agy 또는 Claude는 `https://realtime-info-board.vercel.app/`, `docs/00_과제_요구사항_매핑.md`, `docs/검증안내서.md`, `docs/검증스크린샷/README.md`를 기준으로 독립 최종 검토한다. 최신 원격 성공 세트 `2026-08-26T14-49-09-648Z`와 앞선 실패 세트를 모두 증거로 보존하고, 과제 제출 보고서 작성 시 실제 구현·검증 결과만 사용한다. ESLint 9 EOL은 Next 내장 플러그인의 v10 peer 지원 전까지 남은 비차단 경고로 명시한다.
