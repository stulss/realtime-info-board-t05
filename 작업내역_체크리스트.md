# 작업내역 체크리스트 — 과제 5 (T05)

**프로젝트명:** realtime-info-board-t05 (데이터 신선도 상태 배지 추가)
**단일 진실 공급원(SSOT).** 모든 AI 세션은 이 파일을 가장 먼저 읽고, 작업 후 갱신한다.
**T04 시절 체크리스트:** `docs/T04_작업내역_체크리스트.md` 에 보존.

## 0. 과제 개요
- 베이스: `github.com/stulss/realtime-info-board-t04` `main` = 시작 커밋 `96d2048`
- 추가 기능(정확히 1개): API 응답 `dataStatus`(fresh/stale/unavailable)·`ageSeconds` 를 읽어 화면 우측 상단 상태 배지 표시. 장애 시 마지막 정상값 유지, 배지만 stale.
- 진행 방식: AI A → (인계문서) → AI B. AI B는 대화 전문 없이 저장소+`docs/02_인수인계_문서.md` 만으로 완성.
- 공통 상한: 세션당 15분 / 10회 (`docs/T05_고정검사_및_상한.md`).

## 1. 진행 상황 체크리스트
- [x] 시작 소스 확보 (`96d2048` 클론) + 새 private 저장소 `stulss/realtime-info-board-t05` 생성, 계획 커밋 `fa41377` push (main·feature/t05-freshness-badge)
- [x] 고정 검사 10개 repo 내 동결 (`docs/T05_고정검사_및_상한.md`)
- [x] 아키텍처 기획 (`docs/01_기획.md`)
- [x] 요구사항 매핑 T05-C01~C10 / 루브릭 5카드 (`docs/00_과제_요구사항_매핑.md`)
- [x] 검증 안내서 + 스크린샷 정책 (`docs/검증안내서.md`)
- [x] 인계·보고서·타임라인 스켈레톤 (`docs/02`,`03`,`04`)
- [x] AI A 실행 지시서 (`docs/AI_A_작업지시.md`)
- [x] AI A 구현: `DataStatus` 타입, `freshness.ts`+테스트, `freshness-badge.tsx`, dashboard topbar 마운트, fresh 렌더
- [x] AI A 인계문서 작성 + 커밋 해시 기록 + fresh 스크린샷
- [x] AI B 구현: `use-simulate.ts`, simulate 장애 분기(catch→`displayPayloadAfterFailure`), stale/unavailable 매핑, `tests/e2e/t05-freshness.spec.ts`
- [x] AI B: 고정검사 10개 전부 PASS, T05 스크린샷 5장 (`docs/검증스크린샷/2026-08-29T03-01-00Z/`)
- [ ] 배포(Vercel) + 공개 URL, GitHub public
- [ ] 최종 보고서(PPT→PDF) + 익명 비교표 + 요구사항 매핑 갱신

## 2. 작업 로그
- **[2026-08-29] Claude (Architect/Planner)**: 사용자 프롬프트 교체 확인 — 로컬 `realtime-info-board-t04` 에 있던 이전 T05("검색 필터", 커밋 `57e6c47`, 미푸시)는 폐기 대상으로 판단, 사용자 승인 후 GitHub `realtime-info-board-t04`(`96d2048`)를 `과제5/realtime-info-board-t05/` 로 새로 클론. 브랜치 `feature/t05-freshness-badge`.
- **[2026-08-29] Claude**: 계획 문서를 커밋 `fa41377` ("docs: T05 신선도 배지 기획 및 표준 문서 세트", 17파일 +769/-254)로 만들고, 새 private 저장소 `stulss/realtime-info-board-t05`를 생성해 `main`과 `feature/t05-freshness-badge` 브랜치를 push. 기존 t04는 `upstream` 원격으로 유지. 코드 미변경.
- **[2026-08-29] Claude**: law.md 3-1 표준 문서 세트를 T05 기준으로 구축. T04 동명 문서는 `docs/T04_*.md` 로 보존 복사 후 T05 내용으로 재작성. 고정 검사·상한을 repo 내부로 이관. AI A 실행 지시서 작성. 코드 미변경.
- **[2026-08-29] Codex (AI A)**: `DataStatus`, `toDataStatus`와 단위 테스트, 안정 1초 클록, 접근 가능한 `FreshnessBadge`, dashboard topbar fresh 렌더를 구현. lint/typecheck/build 및 관련 테스트 11개 통과, fresh 화면 증거 저장. 구현 커밋 `90be0ac`.
- **[2026-08-29] Claude Code (AI B)**: `use-simulate.ts`(useEffect + `window.location.search`, `SIMULATE_PLANS`)를 신규 구현하고 dashboard에 simulate 전용 Query를 연결. 실패 시 기존 `displayPayloadAfterFailure()`로 이전 정상값을 유지하며 timeout/auth/ratelimit은 stale, empty는 unavailable로 매핑. `tests/e2e/t05-freshness.spec.ts` 5건 신규 작성. 작업 중 세 가지 문제를 발견해 수정: (1) 인계문서가 "누락"으로 진단한 파일들은 실제로 존재했고, 진짜 원인은 `vitest.config.ts`의 `URL.pathname`이 비ASCII 경로 `과제5`를 퍼센트 인코딩해 `@/` 별칭이 깨진 것 → `fileURLToPath()`로 교체(단위 테스트 29/31 → 45/45), (2) T05-C02가 요구하는 경과 시간 숫자가 fresh 배지에 없어 추가, (3) 클라이언트 클록 지연 시 `경과 -1s`가 나오던 버그를 0 하한 처리 + 회귀 테스트 추가. lint/typecheck/test(45)/build/e2e(14) 전부 통과, T05-C01~C10 전부 PASS. 상한(15분·10회) 내에서 완료(14분·사용자 프롬프트 1회)했으며 내부 도구 실행 호출 수까지 `docs/04_작업_기록.md`에 사실대로 기록.
- **[2026-08-29] Claude Code (AI B)**: `use-simulate.ts`(useEffect + `window.location.search`, `SIMULATE_PLANS`)를 신규 구현하고 dashboard에 simulate 전용 Query를 연결. 실패 시 기존 `displayPayloadAfterFailure()`로 이전 정상값을 유지하며 timeout/auth/ratelimit은 stale, empty는 unavailable로 매핑. `tests/e2e/t05-freshness.spec.ts` 5건 신규 작성. 작업 중 세 가지 문제를 발견해 수정: (1) 인계문서가 "누락"으로 진단한 파일들은 실제로 존재했고, 진짜 원인은 `vitest.config.ts`의 `URL.pathname`이 비ASCII 경로 `과제5`를 퍼센트 인코딩해 `@/` 별칭이 깨진 것 → `fileURLToPath()`로 교체(단위 테스트 29/31 → 45/45), (2) T05-C02가 요구하는 경과 시간 숫자가 fresh 배지에 없어 추가, (3) 클라이언트 클록 지연 시 `경과 -1s`가 나오던 버그를 0 하한 처리 + 회귀 테스트 추가. lint/typecheck/test(45)/build/e2e(14) 전부 통과, T05-C01~C10 전부 PASS. 상한(15분·10회)은 18분·26회로 초과했고 `docs/04_작업_기록.md`에 사실대로 기록.

## 3. 주요 결정 사항
- **작업 위치·저장소**: `과제5/realtime-info-board-t05/`, 새 private 저장소 `github.com/stulss/realtime-info-board-t05` (upstream = 기존 t04). 이전 검색필터 T05 작업 미참조 (사용자 선택).
- **계약 설계**: 기존 6-값 `status` 유지 + 3-값 `dataStatus` 투영. `ageSeconds` 는 서버 값이 아니라 클라이언트 1초 클록으로 재계산 (장애 시 시각 정지·경과 증가, T05-C05).
- **에러 처리 위치**: 클라이언트 렌더 단에서만. 기존 `displayPayloadAfterFailure()` 재사용 (이전 값 유지+stale). 외부 API·DB 코드 무수정.
- **뷰 전략**: 에러 뷰 분리 없음. 단일 뷰에서 배지만 교체 (학생 판단).
- **장애 재현**: URL `?simulate=timeout|auth|ratelimit|empty` → 기존 `/api/verification/failure` 재사용.
- **작업 분할**: AI A = fresh 상태까지, AI B = 장애 분기 + E2E. 인계 실험이 과제 핵심이므로 AI A가 전부 끝내지 않는다.

## 4. 최종 검증 요약
- `npm run lint` — PASS (경고·오류 0)
- `npm run typecheck` — PASS
- `npm test` — PASS 45/45 (10 파일)
- `npm run build` — PASS (11 라우트 생성)
- `npm run test:e2e` — PASS 14/14 (기존 9 + T05 신규 5)
- 고정 검사 T05-C01~C10 — 10/10 PASS. 상세 근거는 `docs/04_작업_기록.md` AI B 칸 표 참조.
- 증거 스크린샷: `docs/검증스크린샷/2026-08-29T03-01-00Z/` (T05_01_fresh, T05_02_stale_timeout, T05_03_stale_auth, T05_04_stale_ratelimit, T05_05_unavailable 외 기존 14장)

## 5. law.md 준수 점검
- [x] law.md 최초 확인, 보호 문서 무수정
- [x] 예상치 못한 로컬 변경(이전 T05) 발견 시 덮어쓰지 않고 사용자에게 확인 후 진행 (law.md §5.4)
- [x] 관련 없는 Skill 전체 열람 회피
- [x] 기존 테스트·문서·설정 무단 삭제 없음, T04 문서 보존 복사
- [x] 구현 단계에서 lint/typecheck/test/build/e2e 수행 (AI A/B)
- [x] 보호 대상(`src/app/api/widgets/**`, `verification/failure/route.ts`, `dashboard.spec.ts`, `T05_고정검사_및_상한.md`, 기존 단위 테스트 기대값) 무수정
- [x] 상한 초과를 숨기지 않고 `docs/04_작업_기록.md`에 사실대로 기록 (law.md §2.5)

## 6. 다음 작업자에게 (Next Prompt)
**다음 작업자: 사용자 (배포·공개 전환) → 이후 Claude (최종 보고서)**

AI A → AI B 인계 실험과 기능 구현은 완료됐다. 남은 일은 배포와 제출물 작성이다.

> `과제5/realtime-info-board-t05` 의 `feature/t05-freshness-badge` 브랜치를 `main` 에 머지하고 Vercel 에 배포해라.
>
> 1. `feature/t05-freshness-badge` → `main` 머지 후 push
> 2. Vercel 프로젝트 연결·배포, 무로그인 공개 URL 확보. 배포 절차와 선택 근거는 `docs/05_배포.md` 를 따르고 실제 주소로 갱신해라
> 3. GitHub 저장소 `stulss/realtime-info-board-t05` 를 public 으로 전환
> 4. 배포된 주소에서 `/` 와 `/?simulate=timeout`, `?simulate=auth`, `?simulate=ratelimit`, `?simulate=empty` 를 직접 열어 배지가 fresh → stale → unavailable 로 바뀌는지 육안 확인 (`docs/검증안내서.md` 3단계)
> 5. `docs/03_최종_보고서_및_비교.md` §1 의 배포 주소·저장소 주소와 §5 도구 선택 기준을 채우고, `docs/00_과제_요구사항_매핑.md` 를 최종 상태로 갱신해라
> 6. 마지막으로 law.md 최종결과보고서 규칙에 따라 PPT 작성 후 PDF 로 변환해라
>
> 코드는 이미 lint/typecheck/test(45)/build/e2e(14) 전부 통과 상태이므로 기능 수정은 필요 없다. `docs/T05_고정검사_및_상한.md` 와 기존 테스트 기대값은 수정하지 마라.
